
  # Social Networking App UI

  This is a code bundle for Social Networking App UI. The original project is available at https://www.figma.com/design/tPitPHEvFDPQWwHgCwA67K/Social-Networking-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  